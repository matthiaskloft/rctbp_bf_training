# API-specific dependencies for serving the RCTBP BayesFlow model

# Web framework
fastapi>=0.100.0
uvicorn[standard]>=0.23.0

# Request/response validation
pydantic>=2.0

# ASGI server dependencies (included in uvicorn[standard])
# httptools
# uvloop  # Linux only
# websockets

# Optional: for development
# watchfiles  # For hot-reload in development
